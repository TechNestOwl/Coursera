// write a function that takes a number parameter in ki
// converts the number to miles. Return the converted number.


function kmToMi(km){

    let conversionToMi = 0;

    conversionToMi = (km * .621);

    return conversionToMi
}
console.log(kmToMi(100));




function celciusToF(celcius){

    let fahrenheit = 0;

    let calculation = (celcius * 9/5) + 32;

    fahrenheit = calculation;

    console.log(fahrenheit);
    return fahrenheit;

};
celciusToF(0);